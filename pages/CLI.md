- ```
  docker start <container>
  docker pull
  docker run #This will pull & start
  docker run --options
  docker stop
  docker ps
  docker exec -it
  docker log
  ```
- run `<option>`
	- ```
	  -d : --detach
	  -p : --ports`
	  ```
-